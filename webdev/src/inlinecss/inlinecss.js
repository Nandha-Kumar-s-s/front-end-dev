const styles = {
    color: 'red',
    fontSize: '16px',
    fontWeight: 'bold'
  };

  const MyComponent = () => {
    return (
      <div style={styles}>
        This is a inline styled div.
      </div>
    );
  };

export default MyComponent;